# React + TypeScript + Vite

## このテンプレートの使い方
1. `npm install` を実行
2. `npm run dev` で起動

AI と一緒に開発する場合は、ルールファイル（[.github/instructions/template.instructions.md](.github/instructions/template.instructions.md)）を参照して進めてください。
Tailwind CSS と shadcn/ui を前提に、`src/` 配下へ機能単位で整理する構成です。
